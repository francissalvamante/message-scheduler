module.exports = {
  name: "update",
  description: "Update a scheduled message.",
  callback: async (client, interaction) => {
    interaction.reply("Update");
  },
  deleted: true,
  // devOnly: Boolean;
  // testOnly: Boolean,
  // options: Object[]
};
